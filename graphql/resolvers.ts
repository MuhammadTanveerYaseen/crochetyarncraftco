/**
 * @deprecated — This file now re-exports from `graphql/resolvers/index.ts`.
 * Import directly from `@/graphql/resolvers` going forward.
 * Kept as a shim so existing imports (`from '@/graphql/resolvers'`) still work.
 */
export { resolvers, resolvers as default } from './resolvers/index';
